module.exports = {
  // The address of the cache (edge) server
  cacheServerAddress: 'localhost',

  // The port the cache (edge) server listens on for storing
  // new bundled
  cacheServerPort: 3092,

  // The port to run the transport node (this server) on
  port: 3093
};
